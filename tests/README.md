# tests/

This directory is reserved for future automated tests, most likely:
- Backend (FastAPI/MQTT) integration tests once `backend/` exists
- Signal-processing unit tests (R-peak detection, HRV calculations) extracted for testing outside the firmware, likely alongside `data-analysis/`
